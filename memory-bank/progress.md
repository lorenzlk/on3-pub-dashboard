# Progress (derived from `progress.jsonl`)

> **Do not edit this file by hand for new events.** Append to `progress.jsonl` only, then refresh this summary.

## Summary

| ID | Type | Date | Title |
|----|------|------|--------|
| mb-001 | decision | 2026-04-02 | Memory Bank + AGENTS.MD as canonical agent context |
| mb-002 | change | 2026-04-02 | Initialize Memory Bank scaffold |
| mb-003 | change | 2026-04-02 | Persona dashboard routes and LivePayload extensions (recap) |
| mb-004 | constraint | 2026-04-02 | Single live API |
| mb-005 | change | 2026-04-02 | Home dashboard uses persona panels + COO role in header |
| mb-006 | change | 2026-04-02 | Unified chart number formatting (currency + counts) |
| mb-007 | change | 2026-04-02 | Remove redundant persona nav links; clarify CRO drill-down |
| mb-008 | change | 2026-04-02 | Move QA modules to bottom of surfaces |
| mb-009 | change | 2026-04-02 | CPO hero metrics: ring gauge + sparklines |
| mb-010 | change | 2026-04-02 | Remove Rollup QA from CPO panel |
| mb-011 | change | 2026-04-02 | COO narrative: widget footprint, activation, demand mix |
| mb-012 | correction | 2026-04-02 | COO metric naming: widget loads ÷ pageviews |
| mb-013 | change | 2026-04-02 | Split widget loads vs SS in-views in UI and docs |
| mb-014 | change | 2026-04-02 | Viewability denominator: widget loads |
| mb-015 | change | 2026-04-02 | Remove SS in-view reach (÷ PVs) from UI |
| mb-016 | change | 2026-04-02 | Polish COO/CPO metric glossary copy |
| mb-017 | change | 2026-04-02 | COO KPI clarity + rollup QA for widget metrics and MoM |
| mb-018 | correction | 2026-04-02 | Hide weekly viewability when chart weeks duplicate loads vs in-views |
| mb-019 | change | 2026-04-02 | COO table: risk for consecutive zero-PV weeks |
| mb-020 | change | 2026-04-02 | Readable grouped COO Data QA alerts |
| mb-021 | change | 2026-04-02 | Home COO: Data QA after Lifetime charts |
| mb-022 | change | 2026-04-02 | CPO Product metrics: formula badges replace corner icons |
| mb-023 | change | 2026-04-02 | Reviewable RPM paired with blended RPM |
| mb-024 | change | 2026-04-02 | Persona panels copy and layout cleanup |
| mb-025 | correction | 2026-04-02 | UI label: Viewable RPM not Reviewable RPM |
| mb-026 | change | 2026-04-02 | metrics.js: broader widget load and SS in-view header aliases |
| mb-027 | change | 2026-04-02 | metrics.js: weekly network trend + weeklyByPublisher + MoM restored |
| mb-028 | change | 2026-04-02 | Home default: group executive overview + one tile per persona |
| mb-029 | change | 2026-04-02 | Group snapshot copy: explicit scopes and shorter UI text |
| mb-030 | correction | 2026-04-04 | Group tile MoM: fix double ×100; gate noisy MoM; DataScopeBanner group layout |
| mb-031 | change | 2026-04-04 | Publisher pie tooltip shows publisher name |
| mb-032 | change | 2026-04-04 | CPO vRPM–RPM gap section: CPO-oriented explainer |
| mb-033 | decision | 2026-04-04 | Revenue semantics: rollup = gross; billing owns net/rev-share |
| mb-034 | change | 2026-04-04 | Memory Bank: cross-link MulaNetworkReporting billing stack |
| mb-035 | change | 2026-04-04 | CEO/CRO: publishers vs Mula split + publisher mulaRev rollup |
| mb-036 | change | 2026-04-04 | buildRollupQa publishers_sum_vs_network: clearer warn + tolerance |
| mb-037 | change | 2026-04-04 | CEO top publisher %: denominator = sum of publisher-tab gross |
| mb-038 | change | 2026-04-04 | QA footers: bottom of home + /dashboard/* persona pages |
| mb-039 | change | 2026-04-04 | CEO/CRO: plain-language money for publishers vs Mula |
| mb-040 | change | 2026-04-06 | Group snapshot CEO tile: prominent publisher vs Mula split |
| mb-041 | change | 2026-04-06 | Mula ÷ gross 2–22% sanity band + UI callout |
| mb-042 | correction | 2026-04-06 | Mula rollup sanity band: 20–22% not 2–22% |
| mb-043 | decision | 2026-04-06 | Mula should be ~20% of gross (target); 20–22% check band |
| mb-044 | decision | 2026-04-06 | Native exception: Mula 20% / publishers 80% of gross native |
| mb-045 | change | 2026-04-06 | Group snapshot: minimal copy |
| mb-046 | change | 2026-04-06 | Group snapshot: minimal redesign with scope hints |
| mb-047 | correction | 2026-04-06 | Retract Mula ~20% of gross target and 20–22% QA/UI band |
| mb-048 | change | 2026-04-06 | NetworkRevenueSplit: strip explainer + native block |
| mb-049 | change | 2026-04-06 | Group CEO tile: Gross Revenue + Net Revenue labels |
| mb-050 | change | 2026-04-06 | CEO panel: gross total above publishers/Mula split |
| mb-051 | change | 2026-04-06 | CPO: replace viewability proxy with publisher mix card |
| mb-052 | change | 2026-04-06 | UI: drop (est.) on money-for-publishers copy |
| mb-053 | change | 2026-04-06 | Group COO/CPO tiles: one-line coverage + viewability hints |
| mb-054 | change | 2026-04-02 | Readability: lighter dark surfaces + secondary text |
| mb-055 | change | 2026-04-07 | CRO: shareable per-publisher URLs + Slack copy |
| mb-056 | change | 2026-04-07 | CRO focused route: publisher drill-down in modal overlay |
| mb-057 | change | 2026-04-08 | RevenueLeaderboard: column headers for $ and MoM % |
| mb-058 | change | 2026-04-08 | Group home: Mula revenue monthly chart + mulaRev in monthlyTotals |
| mb-059 | change | 2026-04-08 | MulaRevenueChart: bar chart instead of area |
| mb-060 | change | 2026-04-08 | RevenueChart: Publisher Revenue Trend labeling |
| mb-061 | correction | 2026-04-12 | Group CEO tile copy + Mula bar tooltip cursor |
| mb-062 | change | 2026-04-12 | Align monthly Mula chart with Network Total row |
| mb-063 | change | 2026-04-12 | COO panel: network-first widgets + publisher section |
| mb-064 | correction | 2026-04-13 | COO: clearer in-progress month vs last month card |
| mb-065 | change | 2026-04-02 | Network rollup Email Rev column (sheet O) alignment |
| mb-066 | change | 2026-04-18 | COO pacing dashboard (sheet parity) |
| mb-067 | change | 2026-04-27 | Improve Sheets migration diagnostics via /api/health and /api/live config validation |
| mb-068 | change | 2026-04-28 | Railway: bind Next start to PORT + 0.0.0.0 |
| mb-069 | change | 2026-04-28 | Sheets client: contextual errors for tab reads |
| mb-070 | change | 2026-04-28 | Railway: separate liveness healthcheck from Sheets diagnostics |
| mb-071 | change | 2026-04-28 | COO lifetime footprint cards adopt CPO hero styling |
| mb-072 | change | 2026-04-28 | Studio dashboard MVP route with dimension slicing |
| mb-073 | change | 2026-04-28 | Studio view upleveled to monthly/quarterly periods + momentum summary |
| mb-074 | change | 2026-04-29 | Ignore local XLSX exports |
| mb-075 | change | 2026-04-29 | Ingest new KVP columns (P/Q) safely |
| mb-076 | change | 2026-04-30 | Bootstrap penske-dashboard repo from Mula dashboard |
| mb-077 | change | 2026-04-30 | Add Memory Bank README |
| mb-078 | change | 2026-04-30 | Add publisher-facing pages for Rolling Stone and THR |
| mb-079 | change | 2026-04-30 | Publisher KPI cards: honest sparkline window + copy |
| mb-080 | change | 2026-04-30 | Publisher surface: viewable RPM + on-screen ad views only |
| mb-081 | change | 2026-04-30 | Home: default THR + publisher switcher |
| mb-082 | correction | 2026-04-30 | PublisherPageClient viewable-only file committed and pushed |
| mb-083 | change | 2026-04-30 | Publisher tiles order + commerce/article KPIs + sparkline polish |
| mb-084 | change | 2026-04-30 | Publisher CTR: denominator in-views not PVs |
| mb-085 | change | 2026-04-30 | Publisher all-time KPIs; remove weekly trend charts |
| mb-086 | change | 2026-04-30 | Publisher hero accents by widget family |
| mb-087 | change | 2026-04-30 | Penske publisher leaderboard under tiles |
| mb-088 | change | 2026-04-30 | Publisher quick-view modal (CRO-style pop-out) |
| mb-089 | change | 2026-04-30 | Home: combined all-time KPIs; sites-only leaderboard; remove publisher switcher |
| mb-090 | change | 2026-04-30 | Strip explanatory helper copy on Penske publisher / quick-view surfaces |
| mb-091 | change | 2026-04-30 | Penske sites list: card grid + visual polish |
| mb-092 | change | 2026-05-01 | NetworkRevenueSplit: remove publisher/Mula dollar tiles |
| mb-093 | change | 2026-05-01 | Remove Mula revenue share from rollup API and UI |
| mb-094 | change | 2026-05-01 | Executive UI polish: section header, spotlight hero, trends shell |
| 2026-05-07 | change | 2026-05-07 | Bootstrap on3-pub-dashboard + full On3 rebrand |

## Details

### mb-001 — decision

Adopt Cline-style Memory Bank under memory-bank/ and AGENTS.MD at repo root as the non-negotiable contract for AI/human agents.

**Why:** Durable, ordered context and append-only progress reduce drift and unsafe refactors.

**Implications:** Agents must read memory-bank files before coding and append progress.jsonl after meaningful work; if Memory Bank and README disagree, Memory Bank wins.

### mb-002 — change

Added memory-bank/projectbrief.md, productContext.md, systemPatterns.md, techContext.md, activeContext.md, progress.jsonl (seeded), and progress.md (derived summary).

**Why:** AGENTS.MD requires an initialized bank; repo had no memory-bank/ at root.

**Implications:** Future sessions start from these files; keep activeContext.md and progress artifacts current after each significant task.

### mb-003 — change

Implemented /dashboard/ceo|coo|cro|cpo with shared shell; extended metrics.js with buildWeeklyByPublisher, buildPublisherMoMComparison, and richer buildWeeklyTrend (PVs, Smart Scroll, blended RPM/vRPM); API returns weeklyByPublisher and publisherMoM.

**Why:** Executive spec: role-specific layouts without new endpoints.

**Implications:** Any new persona metrics should prefer extending metrics.js + LivePayload types before adding parallel APIs; sync-rollup-lib must stay aware of metrics exports.

### mb-004 — constraint

Client data path remains GET /api/live only for dashboard and persona pages.

**Why:** Explicit product constraint from persona implementation spec.

**Implications:** New aggregates belong in metrics.js and the existing JSON response unless product explicitly approves new routes.

### mb-005 — change

ExecutiveRole and DashboardHeader select now include COO; src/app/page.tsx renders CeoPersonaPanel, CooPersonaPanel, CpoPersonaPanel, or CroPersonaPanel by role (replacing inline KPI grids), with shared banners and a Lifetime rollup & charts section below.

**Why:** Single executive home should match focused /dashboard/* content without duplicating layouts.

**Implications:** When changing a persona layout, update the shared panel under src/components/dashboard/personas/ so home and /dashboard/* stay aligned; COO QA anchor on home is id coo-qa-detail for deep links.

### mb-006 — change

Added formatChartCount in src/lib/data.ts; TrafficChart, TrendChart (axes + tooltips), SparklineTrend, RevenueChart, RevenueBreakdownChart, and CroPersonaPanel ComposedChart now use formatCurrencyFull for USD and formatChartCount for volume metrics where applicable.

**Why:** Consistent readable $ (2 decimals) and grouped whole numbers across all dashboard charts.

**Implications:** New Recharts components should import formatters from lib/data rather than ad hoc toFixed or compact formatCurrency for chart surfaces.

### mb-007 — change

Dropped Persona dashboards link from DashboardHeader and footer link on src/app/page.tsx; renamed Publisher detail to Publisher drill-down with subtitles and richer empty-state copy in CroPersonaPanel; RevenueLeaderboard rows use cursor-pointer.

**Why:** Executive views live on /; /dashboard/* remains for bookmarks but need not be advertised; users were unsure what the empty right column was for.

**Implications:** CEO QA deep link still uses /dashboard/coo#qa-detail from StatusBadge until home URL supports persona+hash; PersonaSwitcher on dashboard layout unchanged.

### mb-008 — change

DataQualityBanner renders after Lifetime rollup & charts on src/app/page.tsx (DataScopeBanner stays under title); CeoPersonaPanel network health strip after sparklines; CooPersonaPanel consolidates AlertBanner, QA checks KPI, and check table under a bottom Data QA section with border separator; CpoPersonaPanel moves QA pass rate to a bottom Rollup QA section.

**Why:** Operators want KPIs and primary tables first; QA is supporting detail.

**Implications:** Scroll targets coo-qa-detail / qa-detail unchanged; CEO badge still links to dedicated COO URL for QA anchor.

### mb-009 — change

Added CpoHeroMetricCard with SVG ring, weekly mini-area sparkline, accent styling, and WoW callouts; CpoPersonaPanel replaces flat KpiHeroCard pair for SmartScroll adoption and viewability proxy.

**Why:** Product headline cards felt visually weak vs chart-heavy layouts.

**Implications:** CPO trend series derive from weeklyAgg (network blended ratios); publisher-level proxy headline still uses publisher mean — labels explain the distinction.

### mb-010 — change

Deleted Rollup QA section and QA pass rate KpiHeroCard from CpoPersonaPanel; dropped unused qa-derived state and imports.

**Why:** CPO view should stay product-focused; QA remains on COO path and home DataQualityBanner.

**Implications:** CPO no longer surfaces rollup QA summary; use COO or global Data QA for check lists.

### mb-011 — change

CooPersonaPanel reframed around SmartScroll in-view ÷ PVs (lifetime + weekly trend), partners with loads, network PVs MoM, revenue MoM; added CooDemandMixStrip from kpis.totals channel revenue; widget WoW warn alert; TrendChart supports valueType percent + formatPercentChart in data.ts; home COO heading and PersonaSwitcher COO title updated; productContext COO intent updated.

**Why:** COO questions: maximize widget loads on PVs, activate partners, track affiliate/KVP/video/native demand.

**Implications:** Widget load is proxied by smartScrollViews ÷ totalPVs from rollup; channel mix uses latest-month network totals from buildKpis.

### mb-012 — correction

UI and productContext now state the formula as widget loads ÷ pageviews; metrics.js K.smartScrollViews aliases extended with widget_loads and widget_load (first match wins with existing smart_scroll_views, in_views).

**Why:** Align copy and rollup headers with the business definition of the ratio.

**Implications:** Sheets can expose widget_loads; if both loads and in_views columns exist, ordering in K determines which populates the numerator.

### mb-013 — change

Rollup metrics.js already maps widget_loads vs ss_in_views; client types and COO/CPO/SmartScrollTable/CooPublisherPerformanceTable now treat widgetLoads as eligible serves and smartScrollViews as SS in-views (legacy smart_scroll_views still backfills via pick*). CooPersonaPanel: dual weekly percent series, WoW and activation on loads, extra KPIs for in-view ÷ PVs and in-view ÷ loads. CpoPersonaPanel: three hero cards (loads÷PV, in-view÷PV, vRPM proxy), weekly trend shows widget loads + SS in-views. CpoHeroMetricCard adds emerald accent.

**Why:** Business definitions: pageviews = tags loaded; widget loads = can serve; SS in-views = saw the widget — must not conflate loads and viewability.

**Implications:** Sheets should add split columns when available; dashboards key partner success on widgetLoads; vRPM and in-view rates use smartScrollViews; parseWeekly* defaults widgetLoads from smartScrollViews when absent.

### mb-014 — change

Defined viewability in UI and persona-metrics as SS in-views ÷ widget loads (network and weekly series, COO/CPO heroes, SmartScrollTable viewability column + WoW, COO publisher drill-down). Kept SS in-views ÷ PVs as explicit in-view reach where still shown. Added inViewPctOfWidgetLoads and wowInViewPerLoadsPctDelta.

**Why:** Product: viewability rate should be measured against eligible serves (loads), not only pageviews.

**Implications:** WoW viewability column uses loads-based delta; reach ÷ PVs remains a secondary coverage signal.

### mb-015 — change

Dropped in-view reach KPIs, table column, CPO reach/WoW lines, COO drill-down line, and persona-metrics smartScrollPctOfPvs + wowSmartScrollPctDelta. Product copy no longer describes SS in-views ÷ pageviews.

**Why:** User requested removal of SS in-view reach as a surfaced metric.

**Implications:** Viewability remains SS in-views ÷ widget loads; coverage on PVs is only via widget loads ÷ PVs.

### mb-016 — change

Rewrote intro paragraphs for Pageviews, widget loads, and viewability in CooPersonaPanel and CpoPersonaPanel (parallel sentences, plain language).

**Why:** User asked to clean up glossary wording.

**Implications:** No metric logic changes.

### mb-017 — change

CooPersonaPanel: split Lifetime vs Latest month sections; viewability shows N/A when loads match in-views; hide MoM when month in progress; hide partners card when all have loads; clearer footers. buildRollupQa: rollup_month_in_progress, widget_metrics_legacy_column, widget_loads_match_in_views, extreme_mom_pageviews, extreme_mom_revenue_drop. KpisPayload typing includes pacing.

**Why:** User feedback: mixed lifetime vs latest-month figures and legacy columns made KPIs look wrong; need stronger QA signals.

**Implications:** Data QA banner may show more warns; sheets should add split columns for real viewability.

### mb-018 — correction

CooPersonaPanel viewabilityNotComparable now ORs lifetime duplicate detection with the same weekly slice as the chart: if every week with loads has in-views matching loads within 0.1%, treat as legacy single column and hide cyan series + KPI dash.

**Why:** Lifetime publisher sums could differ slightly while weekly network aggregates still mirrored one column, leaving a flat 100% viewability line.

**Implications:** True split metrics that differ within 0.1% every week could be hidden — tighten relTol if that happens.

### mb-019 — change

persona-metrics: publisherTrafficStallStatus (3+ trailing zero-PV weeks in last 12), mergePublisherPerformanceStatus. CooPublisherPerformanceTable uses merged status for sort, row tint, Risk + No traffic sublabel, drill-down line. CooPersonaPanel AlertBanner entries for stalled publishers.

**Why:** RPM variance stayed green when RPM was flat at zero; McClatchy-style traffic drops were invisible.

**Implications:** Tune min weeks via publisherTrafficStallStatus options if needed.

### mb-020 — change

PersonaAlert supports publishers[] and stallRows[]; AlertBanner renders ops alerts as bordered blocks with RPM chips and zero-traffic table, severity strip, summary line. CooPersonaPanel groups RPM outliers and traffic stalls; clearer QA/widget copy; Data QA section subtitle.

**Why:** Repeated identical RPM bullets were unreadable.

**Implications:** Other panels could reuse PersonaAlert grouping if they adopt AlertBanner.

### mb-021 — change

Extracted CooPersonaDataQaSection; CooPersonaPanel accepts dataQaPlacement panel|external (default panel) and renders Data QA via the section when panel. Home page uses external + CooPersonaDataQaSection after Lifetime rollup & charts, before DataQualityBanner; /dashboard/coo unchanged.

**Why:** User wanted QA checks at the bottom of the home COO flow, not above shared lifetime charts.

**Implications:** Deep link #coo-qa-detail on home now scrolls to the lower section; duplicate useDashboardLive in panel+section on home is acceptable for clarity.

### mb-022 — change

CpoHeroMetricCard uses formulaBadge (mono text) + optional formulaTitle for tooltip/a11y instead of Lucide icons; CpoPersonaPanel passes loads ÷ PVs, in-views ÷ loads, vRPM ÷ RPM.

**Why:** User asked for upper-right marks that are clearer than decorative logos.

**Implications:** New CpoHeroMetricCard instances must pass formulaBadge; extend formulaTitle when the shorthand needs explanation.

### mb-023 — change

CEO/CRO/CPO: KpiHeroCard for Reviewable RPM (kpis.totals.totalVrpm, MoM momTotalVrpmDeltaPct) next to Blended RPM; CEO third SparklineTrend on blendedVrpm; CRO publisher drill-down Reviewable RPM + weekly vRPM spark + ComposedChart reviewableRpm line. buildMonthlyTotalsFromPublisherRows sums smartScrollViews; MonthlyTotalRow.smartScrollViews.

**Why:** User asked for reviewable (vRPM) anywhere blended/RPM appears.

**Implications:** Monthly chart vRPM needs SS in-views in publisher monthly rows; legacy months may show 0 reviewable line until data exists.

### mb-024 — change

PersonaMetricGlossary shared by COO/CPO; CEO/CRO/CPO RPM footers and CEO sparkline labels aligned; CEO KPI grid sm:3 xl:5; COO section headings match CRO/CPO weight, subtitle + glossary, shorter demand/chart blurbs; CPO meta subtitle, deduped first hero body, tighter section lines; CRO shorter leaderboard/drill copy, Blended RPM label in drill, side-by-side weekly RPM sparks; CooPersonaDataQaSection shorter intro.

**Why:** User asked for a fresh pass to clean up each persona view.

**Implications:** Glossary edits live in PersonaMetricGlossary.tsx for COO+CPO only.

### mb-025 — correction

All user-facing strings and CRO chart series name/dataKey use Viewable RPM / viewableRpm; CEO/CPO KpiHeroCard labels and CeoPersonaPanel sparkline; CRO composed chart Line.

**Why:** User asked to drop reviewable wording in favor of viewable.

**Implications:** Product copy should say Viewable RPM; mb-023 ledger title unchanged (append-only).

### mb-026 — change

K.widgetLoads adds widget_load, widget_load_count, total_widget_loads, eligible_*, widget_serves, widget_impressions. K.smartScrollViews prefers ss_in_views and smart_scroll_in_views before in_views and smart_scroll_views.

**Why:** User updated source sheet widget load naming; reduce mismatch with normalized sheet keys.

**Implications:** If loads still read as zero, send the exact column header to add one more alias; avoid duplicate keys mapping two meanings to in_views.

### mb-027 — change

buildWeeklyTrend now sums totalPVs, widgetLoads, smartScrollViews per week; blendedRpm=rev/PVs×1k, blendedVrpm=rev/SS×1k. Re-added buildWeeklyByPublisher (rpm from PVs or sheet, vrpm from SS in-views or sheet) and buildPublisherMoMComparison; module.exports. scripts/verify-metrics-formulas.cjs + npm verify:metrics.

**Why:** API required missing exports; QA on formulas using widget loads vs in-views for vRPM.

**Implications:** Run npm run verify:metrics after changing weekly rollup column maps.

### mb-028 — change

ExecutiveRole adds group; DashboardHeader select and avatar config; src/app/page.tsx defaults to group, renders ExecutiveGroupOverview (CEO revenue+MoM+QA health, COO loads÷PVs, CPO viewability with duplicate-column caveat, CRO blended RPM+MoM+viewable RPM); tile click sets role to full persona. Lifetime charts: group matches CEO/COO/CPO (Revenue+Traffic then breakdown). persona-metrics: loadsAndInViewsAreDuplicate + networkPublisherFootprintTotals; CooPersonaPanel imports shared duplicate helper.

**Why:** Leaders land on whole-network snapshot before drilling into CEO/COO/CPO/CRO.

**Implications:** Group view uses lifetime footprint for COO/CPO tiles (not weekly duplicate OR like COO panel); extend overview if parity with COO viewability guard is required.

### mb-029 — change

ExecutiveGroupOverview: callout explains CEO/CRO = latest month Network Total vs COO/CPO = lifetime Publishers sums; per-tile metricLine labels; CEO/CRO show month label + MoM wording; COO/CPO subs/foot clarified; CPO N/A when duplicate columns; CTA Full {role} dashboard. page.tsx group heading/subtitle and lifetime-section blurb when role=group contrast tiles vs charts. DashboardHeader: Group snapshot select label, fullTitle Snapshot · all roles.

**Why:** User asked for clearer, succinct wording so readers know what each number represents.

**Implications:** If KPI sources change, keep callout and subtitles aligned with metrics.js network vs publisher semantics.

### mb-030 — correction

ExecutiveGroupOverview used (pctFromRatio×100) on CEO/CRO MoM — removed extra ×100. lib/data: formatMomPercentLine (prior revenue floor $25, cap |MoM|>400%), formatRpmMomPercentLine cap only. DataScopeBanner layout=group replaces first bullet so snapshot tiles correctly split Network-month vs lifetime Publishers; removed duplicate callout box from ExecutiveGroupOverview. page passes layout when role=group.

**Why:** Screenshot showed -9638% / +733% MoM from double conversion; What period banner implied all top KPIs were latest-month on group view.

**Implications:** Tune MOM_MIN_PRIOR_REVENUE_USD and MOM_MAX_ABS_PCT in data.ts if legitimate swings get hidden.

### mb-031 — change

PublisherPieChart: custom Recharts Tooltip (RevenuePieTooltip) renders publisher on first line and formatted revenue on second; replaces formatter-only tooltip that hid the slice name.

**Why:** User hover showed only Revenue: $X without which publisher.

**Implications:** Other pie/donut charts can reuse the same pattern if added.

### mb-032 — change

CpoPersonaPanel replaces one-line gap blurb with short copy: different denominators, negative gap interpretation, CPO actions (prioritize product pass topics), widening → check publisher loads/viewability table.

**Why:** User unsure what a CPO should do with the gap table.

**Implications:** If gap definition changes, keep copy aligned with VrpmGapTable formula and vrpmRpmGapTrend.

### mb-033 — decision

productContext: rollup total_rev/totalRevenue is gross-style activity from sheets; commercial flows (demand/network paying pubs or Mula, rev shares, demand partners) and net figures are calculated in billing, not this dashboard. mula_rev is a separate rollup column—not equated to full net in docs. CeoPersonaPanel footnote under Total network revenue states gross vs billing.

**Why:** User clarified that displayed revenue is gross and nets live in billing.

**Implications:** Future gross/net UI or RPM variants should align with billing definitions; do not imply rollup equals Mula net without explicit mapping.

### mb-034 — change

productContext: table for BillingConfig.js, BillingCalculator.js, BillingQA.js, Config.js (gross video fraction), Code.js/rollup + Revenue Share Lookup; note dashboard does not run calculateMonthlyBilling. systemPatterns: sibling repo section pointing to MulaNetworkReporting for settlement math.

**Why:** User added network report folder to workspace; asked to use billing and billing config for context.

**Implications:** Agents with only dashboard clone still rely on written summary; full detail requires MulaNetworkReporting checkout.

### mb-035 — change

NetworkRevenueSplit component: latest-month Publishers (est.) = Total−Mula, Mula share, mix bar, billing footnote; MoM on Mula when cmp.momMulaDeltaPct. CeoPersonaPanel: split below hero, removed duplicate Mula card, grid xl:4. CroPersonaPanel: split with gross header+MoMs, KPI row 3 (RPM,vRPM,PVs). metrics.js buildPublisherTotalsAllTime sums mulaRev; PublisherTotalRow.mulaRev; PublisherTable column Mula share; CRO drill-down gross/pubs est/Mula. ExecutiveGroupOverview CEO tile foot shows pubs est vs Mula. live-payload-types: momMulaDeltaPct on comparison.

**Why:** CEO and CRO need clear view of publisher-attributed vs Mula earnings from rollup.

**Implications:** Sync metrics.js to monorepo when using sync-rollup-lib; publisher est. is not billing net.

### mb-036 — change

metrics.js: fmtUsd helper; publishers_sum_vs_network uses absTol $10 and relTol 1% (vs $1/0.5%) for stacked rounding; warn message includes publisher sum, row count, network total, delta, % of network; detail adds pctOffNetwork.

**Why:** User Data QA showed generic tab-mismatch warning; actionable numbers speed sheet triage without opening JSON only.

**Implications:** Small cross-tab drift under 1% may now pass; larger gaps still warn with explicit $/%; reconcile sheets if warn persists.

### mb-037 — change

buildKpis meta.sumPublisherTotalRevenueLatestMonth; CeoPersonaPanel top share uses that sum (fallback network total if zero) so concentration stays 0–100%; copy says publisher-tab gross.

**Why:** User saw On3 at 106% because top row was divided by Network Total while Publishers tab sum exceeded network (QA mismatch).

**Implications:** Top % is share among publisher rows, not strictly share of network hero until tabs reconcile; sync metrics.js if copied elsewhere.

### mb-038 — change

DashboardPersonaShell renders DashboardPersonaQaFooter after persona children: COO uses external CooPersonaDataQaSection only; CEO DataQualityBanner + PersonaQaCeoStrip; CRO/CPO banner only. coo/page dataQaPlacement=external. PersonaQaCeoStrip extracted; CeoPersonaPanel removes mid-panel health row. page.tsx: unified bottom border block; COO omits duplicate DataQualityBanner; CEO strip at bottom.

**Why:** User asked to move QA to the bottom widget on any page it appears.

**Implications:** CEO QA summary and rollup banner scroll with page end on / and /dashboard/ceo; COO QA no longer inside panel on /dashboard/coo.

### mb-039 — change

NetworkRevenueSplit: title Money for publishers vs money for Mula; card labels Money for publishers (est.) / Money for Mula; clearer explainer; emphasis featured (larger $, padding). CEO: split block above gross hero; hero label Gross network total + extra ties to split. CRO: emphasis featured on split. ExecutiveGroupOverview CEO tile foot: same wording + color. CroPersonaPanel drill-down labels aligned.

**Why:** User still could not see clearly how much money went to publishers vs Mula.

**Implications:** Copy stresses rollup gross split; billing still owns final settlement.

### mb-040 — change

ExecutiveGroupOverview CEO tile: label Gross network total on headline; section Where the gross goes with two-column cards (text-lg amounts, emerald/violet); one-line explainer; month/checks footer only. Extra padding on CEO tile.

**Why:** User screenshot showed split only in tiny grey footer text under gross total.

**Implications:** Group view matches executive clarity of NetworkRevenueSplit; still rollup not billing.

### mb-041 — change

rollupCommercialBands.ts: 2–22% helpers. buildRollupQa mula_share: warn outside band (not ~20% ref); extreme >65%; zero Mula pass; messages point to billing/agreement bases. NetworkRevenueSplit: amber callout when outside band; copy clarifies rollup ≠ contractual take rate. ExecutiveGroupOverview CEO: compact amber note when outside band.

**Why:** User expected agreements ~2–22% margin; dashboard showed ~40% from mula_rev/total_rev — likely column/base mismatch not fixed in code.

**Implications:** Reconcile Monthly Rollup columns with billing; band is product default—adjust constants if deals change.

### mb-042 — correction

rollupCommercialBands MULA_ROLLUP_GROSS_SHARE_MIN_PCT 20; metrics.js MULA_SHARE_BAND_MIN 0.2; QA and UI strings 20–22%. NetworkRevenueSplit amber body references constants.

**Why:** User clarified commercial expectation is 20–22%.

**Implications:** Ratios below 20% now warn; adjust constants if product changes again.

### mb-043 — decision

rollupCommercialBands: MULA_ROLLUP_TARGET_SHARE_PCT 20; copy/QA/mula_share messages state target ~20% of gross; band 20–22% for checks. productContext: mula_rev ≈ 20% × total_rev when aligned. NetworkRevenueSplit + ExecutiveGroupOverview wording.

**Why:** User: Mula revenue should be 20% of gross revenue.

**Implications:** If sheet shows otherwise, treat as rollup formula/column issue; billing remains settlement truth.

### mb-044 — decision

productContext: native is Mula-owned; ~20% of gross native to Mula, ~80% to publishers; other channels differ; headline Mula ÷ total gross is a blend. rollupCommercialBands comment; buildRollupQa mula_share pass/warn mention blend vs native. NetworkRevenueSplit: optional nativeGross prop, cyan callout with est. 20/80 on native gross when kpis.totals.nativeRevenue > 0. CeoPersonaPanel/CroPersonaPanel pass nativeGross. ExecutiveGroupOverview CEO tile: cyan one-liner when nativeRev > 0.

**Why:** User: native is the exception—Mula owns the relationship and pays publishers 80% of native gross; Mula cut is 20% of gross native.

**Implications:** Do not read headline Mula % as the native deal; use native callout + channel mix; QA band stays coarse total-gross check.

### mb-045 — change

page.tsx: no subtitle or DataScopeBanner when role=group; no intro blurb under Rollup section; section title Rollup; hide Data QA block + sheets footer on group. ExecutiveGroupOverview: persona metric lines removed; CEO no Gross network total label; Pubs/Mula split only; no billing/native/amber/foot; COO/CPO numeric foot only; CRO no vRPM foot; CTA Open; MoM empty as em dash.

**Why:** User asked to remove copy for a minimal group snapshot UI.

**Implications:** Full explanations and QA on group home are gone—use persona routes or restore copy if onboarding suffers.

### mb-046 — change

lib/data: formatMomPercentLine + formatRpmMomPercentLine optional compact (no vs month; edge cases as em dash). ExecutiveGroupOverview: per-tile scope line (month + metric or lifetime); compact MoM; CEO gross split section with Publishers/Mula labels; COO/CPO labeled denominators; CRO viewable RPM line; flex cards min-height + Open {role}; hover polish. page.tsx group subtitle one line (Network month vs lifetime).

**Why:** User wanted minimal UI but more helpful than stripped tiles.

**Implications:** Persona dashboards unchanged; MoM compact only where passed; full MoM strings still available for future use.

### mb-047 — correction

Deleted rollupCommercialBands.ts. NetworkRevenueSplit: removed amber target/band callout and product-rule paragraph; native callout is gross $ + deal-specific (no fixed 80/20 est.). buildRollupQa mula_share: removed in-band/out-of-band warns; pass is informational ratio + billing note; kept structural warns (Mula>total, impossible %, >65%, ~0 Mula). productContext: mula_rev bullet no ~20% QA or native 80/20 percentages.

**Why:** User: prior ~20% of gross / check-band framing was wrong.

**Implications:** Do not reintroduce a default Mula% target without explicit product sign-off; ratio stays a sheet diagnostic only.

### mb-048 — change

Removed title/paragraph (Money for publishers vs Mula, billing/take-rate copy); removed nativeGross prop and cyan native section; dropped card footnotes (estimated rollup / our share); shortened % and split labels; monthLabel as small line when CEO layout or under CRO gross header. CeoPersonaPanel/CroPersonaPanel: stop passing nativeGross.

**Why:** User asked to remove long intro and native relationship copy.

**Implications:** Split UI is numbers + bar only; native gross not highlighted in this component.

### mb-049 — change

ExecutiveGroupOverview: CEO scope line uses Gross Revenue (with month); split section title Net Revenue (Publishers / Mula sub-tiles unchanged).

**Why:** User wanted CEO tile wording for headline and the two smaller amounts.

**Implications:** Labels are presentation; rollup math still Total vs Mula column — not billing net unless product maps them.

### mb-050 — change

CeoPersonaPanel: NetworkRevenueSplit with showTotalHeader + momTotalPctPoints (revenue MoM); optional grossHeaderLabel for Gross network total (all activity). Removed duplicate KpiHeroCard gross hero; pacing + rollup note in bordered block below split. NetworkRevenueSplit: grossHeaderLabel prop; larger featured typography when showTotalHeader+featured.

**Why:** User wanted gross network total above money for publishers and Mula on CEO view.

**Implications:** CRO unchanged; CEO no animated gross hero.

### mb-051 — change

CpoPersonaPanel: third CpoHeroMetricCard is Publisher mix — % of publishers with ≥2 of Affiliate/KVP/Video/Native each ≥$25 lifetime gross; count line + flat sparkline (lifetime snapshot; weekly channel splits not in API). CpoHeroMetricCard: amber accent. productContext: CPO bullet mentions publisher mix. Removed vRPM÷RPM proxy card math.

**Why:** User wanted product adoption / diversified-offering signal instead of viewability proxy tile.

**Implications:** Tune MIN_CHANNEL_GROSS_USD or stream list if sheets differ; add weekly channel rows to API for a real trend.

### mb-052 — change

NetworkRevenueSplit, CeoPersonaPanel, CroPersonaPanel drill-down/leaderboard blurb, PublisherTable intro: Money for publishers / publisher without (est.). Channel net (est.) strings unchanged.

**Why:** User asked to remove est from those labels.

**Implications:** Same rollup math; wording only.

### mb-053 — change

ExecutiveGroupOverview: COO sub explains publisher-site coverage (eligible serves vs PVs); CPO sub explains viewability ratio or N/A + sheet columns note.

**Why:** User wanted quick context on what COO vs CPO tiles represent.

**Implications:** Slightly denser tiles; persona panels unchanged.

### mb-054 — change

globals.css :root HSL tokens nudged lighter (background/card/muted/border/input; muted-foreground 64%). Across src/**/*.tsx|ts: bg/from/to-zinc-950 → zinc-900 (incl. opacity suffixed classes); text-zinc-600 → text-zinc-500.

**Why:** User reported UI too dark and body copy hard to read.

**Implications:** Future stark black panels should prefer zinc-900 or semantic tokens; very light zinc-500 body may need zinc-400 if contrast still tight on some monitors.

### mb-055 — change

lib/cro-publisher-url.ts: /dashboard/cro?publisher=… helpers + matchPublisherInTotals. CroPersonaPanel: on /dashboard/cro selection driven by URL (router.replace); invalid param stripped; home uses local selection. RevenueLeaderboard optional shareHref + external link icon per row. Drill-down Copy page link (clipboard full URL). Suspense around useSearchParams.

**Why:** User wanted linkouts for each publisher for Slack canvas/bookmarks.

**Implications:** Share canonical path /dashboard/cro?publisher=<exact sheet name>; encoding handles special characters.

### mb-056 — change

CroPersonaPanel: on /dashboard/cro, leaderboard is full width; drill-down only in fixed z-index overlay (backdrop blur, close via X/backdrop/Escape); body scroll locked. Extracted CroPublisherDrilldownCard. Home CRO keeps two-column inline drill-down.

**Why:** User wanted pub-specific links to open a focused shadow-box style panel on top.

**Implications:** /dashboard/cro UX differs from home CRO; modal shares same metrics component.

### mb-057 — change

RevenueLeaderboard: header row Publisher / amount / MoM %; grid-aligned rows; em dash when MoM missing; title tooltips. CroPersonaPanel passes amountColumnLabel Lifetime vs Month from period toggle.

**Why:** User could not tell what trailing % meant.

**Implications:** If leaderboard reused elsewhere, pass amountColumnLabel or rely on defaults Gross + MoM.

### mb-058 — change

metrics.js buildMonthlyTotalsFromPublisherRows: sum mulaRev per month. MonthlyTotalRow.mulaRev. MulaRevenueChart (violet area, same pattern as RevenueChart). page.tsx group Rollup row: RevenueChart + MulaRevenueChart (replaces TrafficChart slot for group only).

**Why:** User asked for Mula revenue chart on group view.

**Implications:** sync-rollup-lib copies should include metrics change; other roles unchanged in that chart row.

### mb-059 — change

MulaRevenueChart: Recharts BarChart + Bar (violet, rounded top); static SVG fallback as bars with zero baseline when mix of signs. Title Mula revenue by month.

**Why:** User preferred bar chart for Mula monthly series.

**Implications:** None beyond chart type.

### mb-060 — change

RevenueChart card title Publisher Revenue Trend — Revenue trend; subtitle unchanged Monthly Rollup line + month range; loading copy publisher revenue.

**Why:** User wanted explicit publisher revenue trend labeling.

**Implications:** None.

### mb-061 — correction

ExecutiveGroupOverview CEO split: Net Revenue → Gross split + one-line note Publishers+Mula=gross (rollup). MulaRevenueChart: Tooltip cursor={false} (removes full-height grey axis band on hover); Bar background={false}.

**Why:** User saw mismatch: net vs gross labels; Mula chart showed misleading tall grey behind April bar (Recharts default axis cursor).

**Implications:** Other BarCharts may still show axis cursor unless disabled per chart.

### mb-062 — change

metrics.js: overlayNetworkMulaOnMonthlyTotals + buildMonthlyTotalsFromPublisherRows(publishers, network) replaces mulaRev per month from Network tab when that month exists; else keeps publisher sum. GET /api/live passes monthlyNetwork.rows. buildKpis meta.monthlyChartsSource updated. MulaRevenueChart subtitle reflects Network-primary + fallback. Removed CEO/CRO NetworkRevenueSplit footnotes and ExecutiveGroupOverview CEO mismatch blurb.

**Why:** User wanted one consistent Mula $ for a month across CEO headline and Mula-by-month chart.

**Implications:** sync-rollup-lib copies need second arg to buildMonthlyTotalsFromPublisherRows where mirrored; publisher-tab Mula sum still used for lifetime tables and QA publishers_sum_vs_network.

### mb-063 — change

CooPersonaPanel: Network block — weekly network revenue TrendChart + span narrative (Are we growing?), MoM trio card (revenue, PVs, blended RPM vs last month), Channel mix + Publisher mix strips (publisherMoM + sumPublisherTotalRevenueLatestMonth, top-three concentration), weekly loads/viewability TrendChart (network footprint). Partners & publishers below divider: glossary, lifetime footprint KPIs, performance table (per-pub $ copy), RPM chart. CooDemandMixStrip takes monthLabel; CooPublisherMixStrip + PUB_BAR_COLORS; removed duplicate latest-month KPI row and orphan demand strip.

**Why:** User wanted COO questions as widgets: growth, vs last month, revenue diversity, per-publisher $; network at top, publishers as you scroll.

**Implications:** Publisher mix depends on publisherMoM + meta.sumPublisherTotalRevenueLatestMonth; empty strip shows placeholder copy.

### mb-064 — correction

CooPersonaPanel How did we do card: when month open, revenue shows MTD line (day x of y), projected finish from kpis.pacing.projectedRevenue, run-rate % vs full prior month from paceVsLastMonthPct; explainer under title. Pageviews/RPM: MTD snapshot + MoM after close (replaces MoM hidden). Tighter p-3 tiles, no flex-1 stretch. Network section in-progress blurb points to weekly chart + comparison card.

**Why:** User screenshot: MoM hidden + empty tiles was unclear during open month.

**Implications:** Pacing still from buildKpis ratioChange(projected,prevTotal); RPM/PVs MoM remain closed-month only.

### mb-065 — change

Verified pipeline: sheetsClient keyForHeader maps Email Rev → email_rev; buildKpis totals.emailRevenue, monthlyTotals.emailRev, buildRollupQa channel sum, COO demand mix, CRO/CPO charts already consume K.emailRev. Added K.emailRev alias emailrev (one-word header). CooPersonaPanel CHANNEL_COLORS.Email matches RevenueBreakdownChart green.

**Why:** User added Monthly Rollup — Network column O (Email Rev); wanted dashboards correct and no regressions.

**Implications:** If Publishers tab lacks Email Rev, publisher sums and channel QA deltas for Email stay 0 until that tab matches; extend Publishers sheet or accept network-only email gross.

### mb-066 — change

buildKpis pacing extended: projectedTotalPVs, projectedSessions, pace vs prior month for each, prior-month actuals; meta.latestMonthKey and previousMonthKey (YYYY-MM). CooPacingDashboard table (revenue, PVs, sessions) with full USD/count formatting, delta and pacing columns, lastUpdated. CooPersonaPanel: pacing card above weekly/MoM row; vs-last-month tiles are revenue, pageviews, sessions; blended RPM as footer line with MoM when closed.

**Why:** User wanted the Mula Network Report Pacing tab moved into the COO dashboard with clearer MTD vs projected EOM vs prior month across three dimensions.

**Implications:** CEO/CRO unchanged; any client assuming pacing only had revenue fields should use KpisPayload pacing typings.

### mb-067 — change

API routes now validate required Google Sheets env vars and return more actionable error context. /api/health performs a lightweight read of the three configured tabs and reports header/row counts (helps detect wrong spreadsheet ID, missing permissions, or renamed tabs after a workspace migration). /api/live includes missing-env reporting and returns configured spreadsheetId/tab names alongside errors.

**Why:** After migrating the source Google Sheet between Google Workspace accounts, the most common failures are missing service-account access or stale spreadsheet/tab config; clearer endpoints speed up triage.

**Implications:** When a sheet is migrated/duplicated, update GOOGLE_SHEETS_SPREADSHEET_ID and re-share the sheet with GOOGLE_SERVICE_ACCOUNT_EMAIL; use GET /api/health first to confirm access and tab names before debugging KPI/column alias issues.

### mb-068 — change

Updated package.json start script to run next start with -H 0.0.0.0 and -p $PORT (default 3000).

**Why:** Railway healthchecks were failing with service unavailable because the app was not reliably listening on the platform-provided port/host.

**Implications:** Railway deployments should now pass healthcheckPath=/api/health when the service starts; keep start script shell-compatible for Linux builders.

### mb-069 — change

Wrapped googleapis Sheets calls in sheetsClient.js to include spreadsheetId, tab name, range, and HTTP status/message when values.get or spreadsheets.get fails. README diagnostics section notes common causes of Requested entity was not found and Railway private key quoting pitfalls.

**Why:** Deployed environments were still surfacing generic Google errors, making it hard to distinguish wrong spreadsheetId vs missing share vs renamed tab.

**Implications:** When triaging /api/live failures, read the augmented error prefix to identify which configured tab failed; compare against /api/health availableTabs output.

### mb-070 — change

Added GET /api/healthz returning {ok:true,kind:liveness}. Updated railway.toml healthcheckPath to /api/healthz. README now documents healthz vs /api/health (deep Sheets probe).

**Why:** Railway deploy healthchecks were repeatedly service-unavailable while builds succeeded; coupling platform health to Google Sheets reads can prevent the service from ever becoming reachable during startup or transient Sheets/API issues.

**Implications:** Use /api/healthz for Railway healthchecks; use /api/health for sheet migration triage; /api/live remains the dashboard data surface.

### mb-071 — change

Updated CooPersonaPanel lifetime footprint widgets (loads ÷ PVs and viewability) to use CpoHeroMetricCard visual style: gradient cards, formula badges, ring gauges, and weekly sparklines while preserving existing KPI math/copy and WoW notes.

**Why:** User preferred this widget style and asked to reuse it where applicable for better readability and visual consistency.

**Implications:** COO footprint now visually matches CPO hero treatment; keep percentage-based KPI cards on this pattern when adding similar widgets.

### mb-072 — change

Added /dashboard/studio with StudioPersonaPanel: dimension selector (publisher/channel), range selector, publisher metric selector, trend chart, leaderboard, and mix bars using existing /api/live weeklyByPublisher and monthlyTotals payloads. Updated persona switcher/shell routing to include Studio and suppressed persona QA footer on studio route.

**Why:** User requested an investor/advisor-facing surface to slice and evaluate publisher/channel performance over time without adding new APIs yet.

**Implications:** Current studio slicing is bounded by existing payload dimensions (publisher + channel time series); add new source columns and /api/live fields for deeper dimensions (geo/device/source) later.

### mb-073 — change

StudioPersonaPanel now supports monthly or quarterly period grouping (default quarterly), period-window slicing, aggregated publisher metrics by period, and an investor-facing momentum highlight card that emphasizes directional growth (up-and-to-the-right) and current leader.

**Why:** User asked to reduce weeds and make the Studio dashboard more strategic for investors/advisors with cleaner time periods and stronger growth storytelling.

**Implications:** Studio trends are now less noisy and easier to read; publisher metrics are aggregated from weekly rows into period buckets, while channels aggregate from monthly totals into the same buckets.

### mb-074 — change

Added `*.xlsx` to `.gitignore` so downloaded spreadsheet exports stay out of git.

**Why:** Local sheet exports are large, change frequently, and are meant for debugging rather than source control.

**Implications:** Keep using Google Sheets as the source of truth; if a spreadsheet artifact ever needs to be versioned, commit it explicitly or store a sanitized fixture under test/fixtures.

### mb-075 — change

`rollupServer/metrics.js`: added alias keys for `kvp_rev_est` and `kvp_impressions` and sum them into `monthlyTotals` and `publisherTotals` as optional fields; dashboard typings updated with optional `kvpRevEst` and `kvpImpressions`.

**Why:** Source sheet introduced new KVP columns; rollup parsing must stay resilient.

**Implications:** If Studio/COO later needs estimated KVP or impression-based yield, consume those fields from the payload; extend `K` aliases when headers change again.

### mb-076 — change

Initialized standalone repo `lorenzlk/penske-dashboard` from the executive dashboard codebase, updated docs, set `env.example` to the Penske spreadsheet ID, recorded upstream Apps Script id for pipeline context.

**Why:** Isolate customer-facing dashboard work to a dedicated repo and data source while reusing proven ingestion and UI.

**Implications:** Future work references the Penske spreadsheet only; update `metrics.js` / `config.js` if the sheet diverges.

### mb-077 — change

Added `memory-bank/README.md` documenting Cline Memory Bank usage and JSONL-first progress logging.

**Why:** Make the Memory Bank explicit for contributors and agents in this repo.

**Implications:** Point onboarding at `memory-bank/README.md` + `AGENTS.MD`; keep `progress.jsonl` append-only.

### mb-078 — change

Pub-facing `/` and `/p/[slug]` with `/api/publisher/[slug]` reading `Penske Rollup` weekly totals, KPIs, WoW, and charts.

**Why:** Penske workbook is weekly-grain and publisher-facing; align UI to the new rollup schema.

**Implications:** Add publishers to the landing list or a directory endpoint as onboarded; consider removing `/dashboard/*` when publisher UX is final.

### mb-079 — change

Publisher hero cards: dynamic sparkline window (`min(12, available weeks)`), captions that match the window (`This week vs prior week` when N=2, `Not enough history yet` when N=1), revenue badge `This week`, and flat sparkline when all values are equal.

**Why:** Fixed misleading “Last 12 weeks” copy and fake-slope two-point lines when only two weeks of data exist.

**Implications:** Reuse the same caption/window pattern for new KPI tiles; extend copy if MTD/QTD views are added.

### mb-080 — change

Publisher `/p/[slug]`: only Viewable RPM and On-screen ad views hero tiles; weekly trends for those two only; Outcomes and other hero metrics removed. Landing page subtitles aligned.

**Why:** Publishers wanted a narrow, understandable view centered on viewable yield and on-screen volume.

**Implications:** Other KPIs are not shown on the pub surface until intentionally re-added; API still returns full fields for future use.

### mb-081 — change

Home route uses `PublisherDashboardHome` client shell: defaults to Hollywood Reporter, segmented publisher control in sticky header, one embedded `PublisherPageClient` at a time, Full page link follows active slug.

**Why:** Cleaner landing and faster orientation than stacking both publishers.

**Implications:** Extend `PUBLISHERS` in that file when adding titles; first entry remains the default.

### mb-082 — correction

Committed and pushed `PublisherPageClient.tsx` viewable-only layout so remote matches **`mb-080`**.

**Why:** File had been left unstaged while `main` was pushed.

**Implications:** `git pull` on `penske-dashboard` is consistent with the documented publisher surface.

### mb-083 — change

Seven publisher KPI tiles in specified order plus seven weekly trend charts; API adds commerce clicks and article CTR; sparklines upgraded (smooth curve, fill, glow, last-point marker).

**Why:** Match dictated card order and improve mini-chart visuals.

**Implications:** Confirm sheet columns for affiliate clicks and next-article CTR; adjust API mapping if “commerce clicks” should mean total clicks instead.

### mb-084 — change

Publisher API recomputes **affiliate CTR** and **article CTR** (next-page) as percentage of **in-view impressions**, ignoring PV-based sheet CTR fields.

**Why:** Publishers expect CTR vs viewable inventory, not raw PVs.

**Implications:** Article CTR uses **nextpage clicks** numerator; swap in API if another column is the intended article click count.

### mb-085 — change

Publisher API `kpis` is all-time rollups; UI removes weekly Recharts block and sparklines; `showTrailingTrend` on hero cards.

**Why:** Product asked for all-time numbers only, no trend section.

**Implications:** `wow` is always null; first→last week shown under “All time” for context.

### mb-086 — change

Publisher hero cards: cyan = traffic (vRPM, in views, incremental imps), emerald = commerce (revenue, commerce clicks, affiliate CTR), violet = article CTR.

**Why:** User asked for color alignment to widget type.

**Implications:** Change only `PublisherPageClient` accents if product reclassifies a metric.

### mb-087 — change

`/api/publishers/leaderboard` + `PenskePublisherLeaderboard` under publisher tiles; reuses `RevenueLeaderboard`; sheet-scoped list scales past two publishers.

**Why:** Match CRO leaderboard UX for Penske; avoid hardcoding publisher count.

**Implications:** Keep rollup publisher names consistent for slugs; extend home switcher from API later if desired.

### mb-088 — change

CRO-style **`PublisherQuickViewModal`**: large-type all-time snapshot, channel pills, copy link; leaderboard row + home publisher switch + subtitle link; embedded uses parent-hosted modal to avoid stacking.

**Why:** User wanted a readable pop-out for Rolling Stone / Hollywood Reporter (and future pubs).

**Implications:** Confirm `mula_rev` meaning for “Money for publisher” vs “Money for Mula” copy.

### mb-089 — change

**`GET /api/penske/all-time`**: parses full weekly rollup (all publishers), returns same **`kpis`** shape as per-publisher all-time (sums + blended RPM/vRPM + CTR from totals). **`PenskeAllTimeKpiTiles`**: shared seven-tile grid; **`combined`** badge copy for network scope. **`PenskeCombinedHomeBody`**: fetches all-time API on `/`. **`PublisherDashboardHome`**: removed TH/RS tab switcher and embedded per-slug client; header **Penske network** + drill-in copy; quick-view still from leaderboard rows. **`RevenueLeaderboard`**: **`variant`** **`sitesOnly`** (column **Penske sites**, no $/MoM cells, neutral bars, same-tab drill link); **`shareOpenInNewTab`** for CRO vs internal. **`PenskePublisherLeaderboard`**: **`listVariant`** **`sitesOnly`** on home vs full MoM board on **`/p/[slug]`**; **`shareHref`** prefers API **`slug`**. **`PublisherPageClient`**: uses **`PenskeAllTimeKpiTiles`**.

**Why:** User wanted one combined all-time widget across sites and to drop the home publisher switch; drill-in via site name/link.

**Implications:** If duplicate publisher-week rows exist in the sheet, combined totals sum every row (same as per-publisher route). CRO **`/dashboard/cro`** leaderboard unchanged (full variant defaults).

### mb-090 — change

Removed visible explanatory “helper” copy on Penske publisher UX: **`PublisherQuickViewModal`** stat subtitles + header tagline; **`PenskeAllTimeKpiTiles`** long **`badgeTitle`** tooltips and **`combined`** prop; **`PenskePublisherLeaderboard`** intro paragraphs; home and **`/p/[slug]`** header subtitles (including last-updated line on publisher page).

**Why:** User asked to drop rollup-style grey helper text across these screens.

**Implications:** Labels and section headers carry meaning alone; add **`aria-describedby`** later if accessibility review needs more context.

### mb-091 — change

Home **Penske sites** block: **`RevenueLeaderboard`** **`sitesOnly`** uses a **responsive two-column grid** of **site cards** (border, hover, active ring), **lifetime gross** under the title, **cyan→emerald** bar vs top site, **`ArrowUpRight`** control for **`/p/[slug]`**. **`PenskePublisherLeaderboard`** wraps **`sitesOnly`** in one **gradient card** with a **header strip** (no duplicate “Penske sites” label stack).

**Why:** User wanted the list to look better without changing behavior.

**Implications:** Full publisher leaderboard (CRO columns) unchanged.

### mb-092 — change

**`NetworkRevenueSplit`**: removed the two side-by-side **Money for publishers** / **Money for Mula** amount cards; kept gross header (when enabled), **Split** bar with segment tooltips, Mula revenue MoM line when `momMulaPctPoints` is set, QA/footnote blocks. **`CeoPersonaPanel`**: pacing blurb no longer refers to “the card above”; points to the split bar instead.

**Why:** User asked to remove those tiles from the executive UI.

**Implications:** Superseded by **`mb-093`** (entire Mula share surface removed).

### mb-093 — change

**Rollup / API:** `metrics.js` no longer reads `mula_rev` (alias removed). KPI totals, MoM, top publisher, monthly aggregates, publisher lifetime totals, and weekly network trend omit Mula. `mula_share` QA checks removed. `buildChannelBreakdown` returns gross-only rows (net = gross; Mula allocation fields zeroed) but **`GET /api/live` no longer includes `channelBreakdown`**. **UI:** Deleted `NetworkRevenueSplit`, `MulaRevenueChart`, `PublisherTable`, `ChannelEconomicsTable`. CEO/CRO use gross `KpiHeroCard`; group CEO tile has no publisher/Mula split; CRO drill-down and `PublisherQuickViewModal` show gross only. **APIs:** `/api/publisher/[slug]` and `/api/penske/all-time` weekly rows omit `mulaRev`. **Types:** `live-payload-types` and `dashboard-data-context` updated. **Branding:** layout/shell “Penske”; pacing/glossary copy de-Mula’d. **`data.ts`** fixtures stripped of `mulaRev`.

**Why:** User asked to remove all Mula rev share data and tiles.

**Implications:** Spreadsheet `mula_rev` is unused by this app. Downstream must not expect `mulaRevenue`, `mulaRev`, `momMulaDeltaPct`, or `channelBreakdown` on `/api/live`. Monorepo `sync-rollup-lib` merges need care if upstream still relies on Mula fields.

### mb-094 — change

**`KpiHeroCard`**: optional **`spotlight`** — emerald-tinted gradient, hairline top highlight, soft glow, emerald label + icon chip (CEO/CRO gross heroes only). **`DashboardSectionHeader`**: shared gradient vertical bar + title/subtitle (`emerald` | **`cyan`** accent). **`CeoPersonaPanel`**: section header; spotlight gross card; pacing block with light backdrop blur; 12-week **`SparklineTrend`** grid inside one **Trailing 12 weeks** panel (inner shadow + ring). **`CroPersonaPanel`**: same header + spotlight gross. **`PenskeAllTimeKpiTiles`**: cyan-accent header for combined home KPI section.

**Why:** Visual interest without changing metrics, routes, or grid structure.

**Implications:** Other dashboards can adopt **`DashboardSectionHeader`** for consistency; use **`spotlight`** sparingly so the gross hero stays visually primary.

